export interface FAQItem {
  question: string;
  answer: string;
}

export interface RoadmapStep {
  id: string;
  title: string;
}

export interface Facility {
  id: string;
  title: string;
  description: string;
}
